<% layout('layouts/boilerplate.ejs') %>

<h1>All Campgrounds</h1>
<a href="/campgrounds/new">Add new campground</a>

<ul>
	<% for(camp of campgrounds) { %>
	<li><a href="/campgrounds/<%= camp.id %> "><%= camp.title %></a> -- <%= camp.location %></li>
	<% } %>
</ul>
